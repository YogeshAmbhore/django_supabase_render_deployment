version: '3.9'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/postgres"
      DEBUG: "True"
    volumes:
      - .:/app
